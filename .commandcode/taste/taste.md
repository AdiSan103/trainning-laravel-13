- Expects the assistant to read project-level context files (`.claude`, `CLAUDE.md`) for knowledge and project setup before beginning any task. Confidence: 0.85
- Prefers public-facing project documentation and articles to be written in a journalistic, narrative style — telling the story behind the code rather than dry technical specs. Confidence: 0.8
- Prefers communication in Bahasa Indonesia (Indonesian language) for all interactions, including technical discussions and code explanations. Confidence: 0.9
- Prefers a structured, methodical workflow: first explore the codebase, create a comprehensive task list (todo), then systematically work through each item (migration → models → controllers → config → views → factories → tests → verify), including fixing test failures before declaring completion. Confidence: 0.7
- Prefers code (controllers, models, and other PHP files) to be annotated with detailed, line-by-line comments in Bahasa Indonesia — explaining each function's purpose, the reasoning behind the code, and Laravel mechanics — as a learning aid. When the user says "implement juga di [folder]", they want the same comprehensive commenting style applied to that folder's files too. For Blade/view files (`resources/views/`), comments should use Blade syntax (`{{-- --}}`) and be placed ONLY on Laravel/PHP code (directives like `@csrf`, `@auth`, `@error`, helpers like `route()`, `old()`, `Auth::user()`, etc.), not on HTML/CSS/JS markup. Confidence: 0.95

- Prefers git commits to use clean, semantic commit messages following conventional commit format (e.g., `type: short description` with a detailed body explaining what and why). Confidence: 0.9
